'use strict';

/**
 * config/supabase.js
 *
 * Phase 0 STUB: Supabase admin client.
 *
 * In Phase 2, this will:
 *   - Import @supabase/supabase-js
 *   - Create an admin client using the service-role key
 *   - Export the client for use in repositories and middleware
 *
 * The service-role key MUST only exist in backend environment variables.
 * It must NEVER be sent to the frontend.
 */

// TODO (Phase 2): Uncomment and configure when Supabase is set up
// const { createClient } = require('@supabase/supabase-js');
// const config = require('./index');
//
// const supabaseAdmin = createClient(
//   config.SUPABASE_URL,
//   config.SUPABASE_SERVICE_ROLE_KEY,
//   { auth: { autoRefreshToken: false, persistSession: false } }
// );
//
// module.exports = supabaseAdmin;

module.exports = null; // placeholder
