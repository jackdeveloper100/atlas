'use strict';

/**
 * config/stripe.js
 *
 * Phase 0 STUB: Stripe SDK instance.
 *
 * In Phase 2, this will:
 *   - Import the stripe package
 *   - Initialise with the secret key from config
 *   - Export the configured client for use in subscription services
 *
 * The secret key MUST only exist in backend environment variables.
 */

// TODO (Phase 2): Uncomment when Stripe is configured
// const Stripe = require('stripe');
// const config = require('./index');
// module.exports = new Stripe(config.STRIPE_SECRET_KEY, { apiVersion: '2024-06-20' });

module.exports = null; // placeholder
